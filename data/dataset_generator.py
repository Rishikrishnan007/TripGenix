import pandas as pd\nimport random\nfrom datetime import datetime, timedelta\n\ndef generate_tourists(num):\n    names = [f'Tourist {i}' for i in range(1, num + 1)]\n    ages = [random.randint(18, 70) for _ in range(num)]\n    countries = [random.choice(['USA', 'India', 'Canada', 'UK', 'Australia', 'Germany']) for _ in range(num)]\n    return pd.DataFrame({'Name': names, 'Age': ages, 'Country': countries})\n\ndef generate_destinations(num):\n    destinations = [f'Destination {i}' for i in range(1, num + 1)]\n    descriptions = [f'Description of {dest}' for dest in destinations]\n    categories = [random.choice(['Nature', 'Adventure', 'Cultural', 'Historical', 'Urban']) for _ in range(num)]\n    return pd.DataFrame({'Destination': destinations, 'Description': descriptions, 'Category': categories})\n\ndef generate_interactions(num, tourist_count, destination_count):\n    interaction_types = ['Like', 'Share', 'Comment']\n    interactions = []\n    for _ in range(num):\n        tourist_id = random.randint(1, tourist_count)\n        dest_id = random.randint(1, destination_count)\n        interaction = random.choice(interaction_types)\n        interactions.append({'Tourist_ID': tourist_id, 'Destination_ID': dest_id, 'Interaction_Type': interaction})\n    return pd.DataFrame(interactions)\n\ndef generate_travel_history(num, tourist_count):\n    travel_records = []\n    for _ in range(num):\n        tourist_id = random.randint(1, tourist_count)\n        visit_date = datetime.utcnow() - timedelta(days=random.randint(1, 365))\n        travel_records.append({'Tourist_ID': tourist_id, 'Visit_Date': visit_date.strftime('%Y-%m-%d'), 'Duration_Days': random.randint(1, 30)})\n    return pd.DataFrame(travel_records)\n\n# Generate data\ntourists_df = generate_tourists(100000)\ndestinations_df = generate_destinations(10000)\ninteractions_df = generate_interactions(500000, 100000, 10000)\ntravel_history_df = generate_travel_history(200000, 100000)\n\n# Save to CSV\ntourists_df.to_csv('data/tourists_data.csv', index=False)\ndestinations_df.to_csv('data/destinations_detailed.csv', index=False)\ninteractions_df.to_csv('data/interactions.csv', index=False)\ntravel_history_df.to_csv('data/travel_history.csv', index=False)\n